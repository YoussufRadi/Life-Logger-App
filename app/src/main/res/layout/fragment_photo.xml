<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.winterproject.youssufradi.life_logger.Photo.PhotoFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="465dp"
        android:orientation="vertical">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/bg_register"
            android:titleTextColor="@color/bg_register"
            android:text="Custom Galleries"
            android:theme="?attr/actionBarTheme"
            android:textAlignment="center"
            android:textSize="18dp"
            />

        <GridView
            android:id="@+id/photo_custom"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:numColumns="2"
            android:horizontalSpacing="5dp"
            android:layout_weight="1" />

        <TextView
            android:layout_width="match_parent"
            android:layout_marginTop="@dimen/activity_horizontal_margin"
            android:layout_height="wrap_content"
            android:background="@color/bg_register"
            android:titleTextColor="@color/bg_register"
            android:text="MetaData Generated Galleries"
            android:theme="?attr/actionBarTheme"
            android:textAlignment="center"
            android:textSize="18dp"
            />

        <GridView
            android:id="@+id/photo_generated"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:numColumns="2"
            android:horizontalSpacing="5dp"
            android:layout_weight="1" />
    </LinearLayout>

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/photo_add_new"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_margin="@dimen/fab_margin"
        android:backgroundTint="@color/bg_main"
        app:srcCompat="@android:drawable/ic_input_add" />

    <android.support.design.widget.FloatingActionButton
        android:id="@+id/photo_refresh"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginBottom="15dp"
        android:layout_marginRight="65dp"
        android:backgroundTint="@color/bg_main"
        app:srcCompat="@android:drawable/ic_menu_rotate" />

</FrameLayout>

